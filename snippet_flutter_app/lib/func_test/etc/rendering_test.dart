import 'package:flutter/material.dart';

class RenderingTest extends StatelessWidget {
  const RenderingTest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}